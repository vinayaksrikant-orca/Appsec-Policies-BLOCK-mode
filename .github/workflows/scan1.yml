name: Sample Orca Container Build & Scan

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Allows you to trigger this manually from the Actions tab

jobs:
  orca-container_scan:
    name: Orca Container Image Scan
    runs-on: ubuntu-latest
    
    # REQUIRED PERMISSIONS: Prevents the "Resource not accessible" error
    permissions:
      contents: read 
      security-events: write
      actions: read

    env:
      PROJECT_KEY: testing-project-vinayak # <--- REPLACE WITH YOUR ACTUAL PROJECT KEY
      IMAGE_NAME: my-vulnerable-test:latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. Build an intentionally highly vulnerable image
      - name: Create Sample Dockerfile & Build Image
        run: |
          # Use an ancient, unsupported base image
          echo "FROM python:3.6-alpine" > Dockerfile
          
          # Add some standard OS-level packages
          echo "RUN apk add --no-cache curl openssl bash git tar" >> Dockerfile
          
          # Force the installation of famously vulnerable Python application packages
          echo "RUN pip install django==1.11.2 requests==2.20.0 urllib3==1.24.1" >> Dockerfile
          
          docker build -t ${{ env.IMAGE_NAME }} .

      # 2. Run the Orca Scanner on the image we just built
      - name: Run Orca Container Image Scan
        uses: orcasecurity/shiftleft-container-image-action@v1
        with:
          api_token: ${{ secrets.ORCA_SECURITY_API_TOKEN }}
          project_key: ${{ env.PROJECT_KEY }}
          image: ${{ env.IMAGE_NAME }}
          format: "sarif,json,table"
          output: "results/"
          display_name: "Orca Container Scan"

      # 3. Upload the SARIF report to GitHub's Security Tab
      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v3
        if: always() # Ensures it uploads even if the scan finds vulnerabilities and "fails"
        with:
          sarif_file: results/

      # 4. Save the JSON and SARIF files as a downloadable zip file
      - name: Upload Scan Results as Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: orca-scan-results
          path: results/

      
